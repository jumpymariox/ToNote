<note wx:for="{{notes}}" id="{{item.id}}" note="{{item}}" bindtap="redirectToNotePage" bindlongpress="readyToEditNotes" editting="{{editting}}" selected="{{selectedNoteMap[item.id]}}" bind:selectNode="selectNode"></note>