<note wx:for="{{notes}}" id="{{item.id}}" note="{{item}}" bindtap="redirectToNotePage" editting="{{editting}}" selected="{{selectedNoteMap[item.id]}}" bindlongpress="readyToEditNotes" bind:selectNode="selectNode">
</note>